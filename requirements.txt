# QuickWhistle dependencies (Python 3.11+)
# Stack is LOCKED per the build brief — do not swap libraries without asking.

# --- PDF extraction ---
PyMuPDF==1.24.10          # `fitz` — primary text extraction
pdfplumber==0.11.4        # table-like pages
pytesseract==0.3.13       # OCR fallback only (requires system Tesseract binary)
Pillow==10.4.0            # image handling for OCR

# --- Orchestration (kept light) ---
llama-index-core==0.11.14

# --- Embeddings (local, free) ---
sentence-transformers==3.1.1   # model: BAAI/bge-small-en-v1.5

# --- Vector store (local, persistent) ---
chromadb==0.5.5

# --- Hybrid retrieval ---
rank-bm25==0.2.2          # keyword/BM25 side of hybrid search

# --- Generation LLM adapters (swappable via config) ---
anthropic==0.116.0             # default: Claude via the Anthropic API (claude-haiku-4-5)
google-generativeai==0.7.2     # alternative: Gemini (free tier). Pinned to 0.7.x to
                               # satisfy langchain-google-genai 1.0.10 (needs >=0.7,<0.8).
ollama==0.3.3                  # alternative: local llama3.1 / llama3.2

# --- Evaluation ---
ragas==0.1.21             # faithfulness, answer relevancy, context recall
datasets==2.21.0          # RAGAS needs this for its dataset format
langchain-google-genai==1.0.10   # wraps Gemini as a LangChain LLM for RAGAS judging
                                 # (1.0.x is required: needs google-generativeai>=0.8,
                                 #  matching the 0.8.2 pin above. Leaving it unpinned makes
                                 #  pip pick 0.0.x, which demands google-generativeai<0.4 -> conflict.)

# --- UI ---
streamlit==1.38.0

# --- Config/secrets ---
python-dotenv==1.0.1

# --- Optional (demo screenshots only; not needed to run the app) ---
# pip install playwright && python -m playwright install chromium
# playwright
